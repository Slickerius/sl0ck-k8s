# Least-privilege identity used by the IrnusDL GitHub Actions deploy job.
# It can restart the irnusdl Deployment and watch the result. Nothing else.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: github-deployer
  namespace: irnusdl
---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: github-deployer
  namespace: irnusdl
rules:
  # `kubectl rollout restart` reads then patches the Deployment.
  - apiGroups: ["apps"]
    resources: ["deployments"]
    verbs: ["get", "patch"]
    resourceNames: ["irnusdl"]
  # `kubectl rollout status` watches; watch cannot be name-restricted,
  # so this stays namespace-scoped instead.
  - apiGroups: ["apps"]
    resources: ["deployments"]
    verbs: ["list", "watch"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: github-deployer
  namespace: irnusdl
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: github-deployer
subjects:
  - kind: ServiceAccount
    name: github-deployer
    namespace: irnusdl
---
# k8s 1.24+ no longer auto-creates SA token secrets, and CI needs a
# non-expiring one, so it is requested explicitly.
apiVersion: v1
kind: Secret
metadata:
  name: github-deployer-token
  namespace: irnusdl
  annotations:
    kubernetes.io/service-account.name: github-deployer
type: kubernetes.io/service-account-token
