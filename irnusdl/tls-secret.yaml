apiVersion: v1
kind: Secret
metadata:
  name: irnusdl-tls
  namespace: irnusdl
type: kubernetes.io/tls
data:
  tls.crt: ""  # base64 of fullchain.pem — see deployment guide
  tls.key: ""  # base64 of privkey.pem  — see deployment guide
